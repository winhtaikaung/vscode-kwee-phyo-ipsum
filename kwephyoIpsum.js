function generator({count,unit,random}){
    const options = {count:!isNaN(count) ||1,
    unit:unit||'sentences',
    random:false}
    
    for(i=0;i<options.count;i++){

    }
}
module.exports = generator